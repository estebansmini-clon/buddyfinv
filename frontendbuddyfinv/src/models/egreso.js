export class egreso {
    constructor(id, costo, observacion, idMetodoPago, idPropietario, idTipoEgreso) {
     this.id = id;
     this.costo = costo;
     this.observacion = observacion;
     this.idMetodoPago = idMetodoPago;
     this.idPropietario = idPropietario;
     this.idTipoEgreso = idTipoEgreso;
    }
 }